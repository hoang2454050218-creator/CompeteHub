# Frontend Environment Variables
# Copy to .env.local for local development

# API base URL (relative path uses Vite proxy in dev)
VITE_API_URL=/api/v1

# WebSocket URL (leave empty to use window.location.origin)
VITE_WS_URL=
